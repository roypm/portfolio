---
interface Props {
  href: string;
  class?: string;
  "aria-label"?: string;
}

const { href, class: className, "aria-label": ariaLabel } = Astro.props;
---

<a href={href} class={className} target="_blank" rel="noopener noreferrer" aria-label={ariaLabel}>
  <slot />
</a>
